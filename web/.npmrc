# No dependency may run code at install time (`npm ci` / `npm install`).
# The only package with an install script here is tesseract.js, whose
# postinstall only prints a funding notice. `npm run <script>` still works.
# See docs/INCIDENT-2026-09-25.md.
ignore-scripts=true
